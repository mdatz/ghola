import { Blockquote } from '@mantine/core';

export function Quote() {
    return (
        <Blockquote cite="– Linda Lam">
            One good conversation can shift the direction of change forever.
        </Blockquote>
    );
}